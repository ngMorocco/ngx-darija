import { RerenderDirective } from './rerender.directive';

describe('RerenderDirective', () => {
  it('should create an instance', () => {
    const directive = new RerenderDirective();
    expect(directive).toBeTruthy();
  });
});
